version: '2'
services:
  redis:
    image: redis

  app:
    build: .
    command: yarn start
    links:
      - redis
    ports:
      - "3000:3000"
    volumes:
      - "./:/usr/app"
      - "/usr/app/node_modules"
    environment:
      - "REDIS_HOST=redis"
      - "COOKIE_SECRET=secret"
